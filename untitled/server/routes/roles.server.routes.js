/**
 * Created by User on 03-Nov-15.
 */

'use strict';
/*
module.exports = function(app) {
    var role = require('../controllers/roles.server.controller');
    app.route('/roles')
        .get(roles.get)
        .post(roles.create);

    app.route('/roles/:_id')
        .get( roles.read)
        .put(roles.update)
        .delete(roles.delete);

    // Finish by binding the article middleware
  //  app.param('categoryId', categories.getByID);
};
    */